import { createRequire } from 'module'
const require = createRequire(import.meta.url)
const mod = require('./scraper.js')

export const uploader = mod.uploader
export const downloader = mod.downloader
export const search = mod.search
export const tools = mod.tools
export const maker = mod.maker

export default mod